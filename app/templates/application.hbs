<div class="container">
  {{! Global Modal }}
  <div id="ember-bootstrap-wormhole"></div>
  <div class="d-flex justify-content-between align-items-center">
    <span class="fs-1">🐹</span>
    <h2 class="pt-3 text-center fw-bold fs-1">Ember Media </h2>
    <ProfileSwitcher @users={{this.model}} />
  </div>
  {{outlet}}
</div>