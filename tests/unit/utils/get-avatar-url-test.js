import getAvatarUrl from 'ember-test/utils/get-avatar-url';
import { module, test } from 'qunit';

module('Unit | Utility | getAvatarUrl', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = getAvatarUrl();
    assert.ok(result);
  });
});
